# CLAUDE.md

This file is loaded automatically by Claude Code at the start of every session.

**→ Read [`AGENTS.md`](./AGENTS.md) before doing anything else.**

`AGENTS.md` is the single source of truth for how any AI agent must operate in this repository. It defines:

- The mandatory reading order
- The three-layer documentation model (vision / system / state)
- The interview protocol when the project is underspecified
- Writing and commit rules

This `CLAUDE.md` exists only so Claude Code auto-loads the pointer. Do not duplicate content here. Do not edit this file to add instructions — edit `AGENTS.md` instead.
