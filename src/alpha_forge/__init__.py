"""Alpha Forge — laboratório de pesquisa e validação de estratégias de trading cripto.

Este pacote é um esqueleto no momento. Consulte `vision/` para o alvo,
`system/` para o que já existe, e `STATE.md` para o estado atual.
"""

__version__ = "0.0.0"
