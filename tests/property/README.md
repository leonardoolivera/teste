# `tests/property/`

Property-based tests com `hypothesis`. Ainda não há nenhum.

Alvo prioritário: **corpus anti-lookahead** — gerar estratégias sintéticas com lookahead deliberado e garantir que o enforcement detecta 100% (NFR em `vision/03-architecture.md`).

Marque com `@pytest.mark.property`.
