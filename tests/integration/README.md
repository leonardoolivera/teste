# `tests/integration/`

Testes end-to-end do pipeline. Ainda não há nenhum — aguardando implementação dos módulos de domínio.

Marque testes pesados com `@pytest.mark.slow` para permitir `pytest -m "not slow"` em dev loop.
