# `scripts/`

**Utilitários transitórios.** Fluxos recorrentes devem migrar para `src/alpha_forge/cli/` ou módulos internos assim que estabilizarem.

Regra: nada aqui é parte da API pública. Se um script virar crítico, ele é promovido.
