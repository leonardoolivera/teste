# decisions/

Architecture Decision Records. One decision per file. Immutable once committed.

## Format

`NNNN-short-slug.md` — sequential number, lowercase, dash-separated.

Examples:
- `0001-use-django-over-fastapi.md`
- `0002-postgres-not-mongo.md`
- `0003-token-auth-not-jwt.md`

## Rules

1. **Never edit an ADR after it is merged.** If a decision is revisited, write a new ADR that supersedes it, and mark the old one as superseded at the top.
2. **One decision per file.** If you're bundling two decisions, split them.
3. **Keep ADRs short.** One page is usually too long. Half a page is often right.
4. **Write the ADR before or during the change, not after.** An ADR written weeks later is a rationalization.

## Template

See [`_TEMPLATE.md`](./_TEMPLATE.md). Copy it, renumber, rename.

## Index

_(Keep this list sorted and up to date. One line per ADR.)_

- [0001 — Foundational stack and architecture](./0001-foundational-stack-and-architecture.md) — Python 3.12 + vectorbt + 7 módulos com acoplamento frouxo; src-layout; uv/ruff/pyright/pytest; deploy local WSL2/Linux/macOS.
- [0002 — Anti-lookahead as infrastructure](./0002-anti-lookahead-as-infrastructure.md) — ordem temporal estrita + guardião de causalidade como infra + teste property-based obrigatório; motor próprio no núcleo mínimo é tática inicial, compatível com direção vectorbt.
- [0003 — Validation: walk-forward causal + Monte Carlo sobre trades](./0003-validation-walk-forward-and-monte-carlo.md) — `validation/walk_forward.py` divide dataset em N folds contíguos disjuntos sem tuning (fold 0 pulado por falta de train prévio; `scheme="rolling"` ou `"expanding"`); `validation/monte_carlo.py` reamostra PnLs de trades com `seed` obrigatório; `MonteCarloSummary` com percentis fixos `{5,25,50,75,95}`; composite scoring, ranking, stress de custos e tuning permanecem deferred.
- [0004 — Minimal risk policy](./0004-minimal-risk-policy.md) — núcleo mínimo só tem `RiskBudget` e `fixed_fractional_position_sizing`; sem sizing por volatilidade, sem equity guard, sem composite budgets.
- [0005 — Dataset versioning and manifest](./0005-dataset-versioning-and-manifest.md) — Parquet em `data/processed/` + manifesto `data/datasets.yaml` com sha256 e gaps declarados; `dataset_id` imutável; gap não declarado bloqueia carregamento.
- [0006 — Minimal execution cost model](./0006-minimal-execution-cost-model.md) — `CostModel` com taker fee em bps + slippage linear por notional/capital; aplicado no preço de execução; argumento obrigatório em `run_backtest`; sem maker/funding/spread nesta fase.
- [0007 — Minimal backtest metrics](./0007-minimal-backtest-metrics.md) — `BacktestMetrics` com `total_pnl`, `trade_count`, `hit_rate` (`None` quando sem trades) e `max_drawdown` (fração); vive em `backtest/metrics.py`; `ranking/scoring/` fica para depois.
- [0008 — First real strategy: causal MA crossover (long-only)](./0008-first-real-strategy-ma-crossover.md) — SMA curta × SMA longa sobre `close`; entrada em cruzamento para cima, saída em cruzamento para baixo; long-only; warm-up = `HOLD` até `long_window + 1` barras; stateless; sem stops/targets/filtros nesta fase.
- [0009 — First real dataset: BTC/USDT 1h, 180 days, Binance Vision dumps](./0009-first-real-dataset-binance-vision.md) — Binance Vision ZIPs mensais (sem `ccxt`); recorte inicial `btcusdt_1h_20250705_20251231_binance_spot`; multi-asset **por design** (nenhum hardcode de símbolo em `src/`); script de ingestão multi-símbolo desde o dia 1; gaps reais declarados um a um com razão factual; teto de 3 gaps / 48h.
- [0010 — Property-based test: cost monotonicity](./0010-cost-monotonicity-property-test.md) — `hypothesis` sobre `run_backtest`: fixado o cenário, se `cost_high ≥ cost_low` componente a componente e `trade_count > 0`, então `final_equity_high ≤ final_equity_low + tolerance`. Só em `final_equity`; fills podem divergir; estratégia de referência = MA crossover 20/50 sobre sintético seminal.
- [0011 — Donchian breakout strategy (long-only)](./0011-donchian-breakout-strategy.md) — breakout assimétrico em `high[t-1]`/`low[t-1]` sobre janelas independentes (`entry_window` default 20, `exit_window` default 10); long-only; stateless; EXIT avaliado antes de ENTER_LONG; redução a no-op por sinais redundantes é responsabilidade do engine; caracterização inicial em dataset real é observação, não validação.
- [0012 — MA crossover: short side opt-in](./0012-ma-crossover-short-side.md) — parâmetro `long_only: bool = True` na MA (default preserva ADR-0008 bit-a-bit); `long_only=False` emite `ENTER_SHORT` em cross-down; engine ganha ramo reverse-on-signal (posição aberta + sinal oposto → fecha + reabre em `t+1 open`, custo duplo); CLI ganha `--no-long-only`; Donchian permanece long-only.
- [0013 — Donchian breakout: short side opt-in](./0013-donchian-short-side.md) — parâmetro `long_only: bool = True` na Donchian (default preserva ADR-0011 bit-a-bit); `long_only=False` emite `ENTER_SHORT` em breakout bearish; reusa reverse-on-signal do engine (ADR-0012) sem alterar `engine.py`; ambos rompimentos simultâneos no modo simétrico → `ENTER_SHORT`; CLI `--long-only/--no-long-only` passa a cobrir também `--strategy donchian`.
- [0014 — Stress de custos sistematizado](./0014-systematic-cost-stress.md) — `validation/cost_stress.py` com `CostPerturbation` (deltas absolutos aditivos em bps, não-negativos) + `CostStressCell` + `CostStressReport`; N+1 backtests (baseline + perturbações); monotonicidade ADR-0010 assertada por cenário; sem CLI, sem persistência, sem flags de fragilidade; módulo vive dentro de `validation/` por falta de irmão (`stress/` raiz fica para ADR futura).
- [0015 — Persistência de relatórios de validação](./0015-validation-report-persistence.md) — `validation/persistence.py` grava/carrega `WalkForwardFold[]`, `MonteCarloSummary`, `CostStressReport` em `results/validation/<run_id>/{walk_forward,monte_carlo,cost_stress}.json`; envelope JSON com `schema_version="1"` (string); sobrescrita permitida; sem compressão, sem migração, sem CLI; round-trip bit-a-bit testado; precursor mínimo do futuro `ranking/`.
- [0016 — CLI de validação: subcomando `validate`](./0016-validation-cli-subcommand.md) — `alpha-forge validate --run-id <str>` roda pipeline completo walk-forward + Monte Carlo + cost_stress e persiste via ADR-0015; flags `--n-folds`/`--scheme`/`--train-fraction`/`--min-test-bars` (walk-forward), `--mc-resamples`/`--mc-seed` (Monte Carlo), `--stress label:fee:slip` repetível (perturbações), `--skip-monte-carlo`/`--skip-cost-stress` tornam cada artefato opcional; `run_id` é string opaca obrigatória, sem timestamping implícito; sobrescrita silenciosa; stdout é humano, JSON persistido é o contrato; sem descoberta/comparação de corridas, sem grid, sem metadados de corrida nesta fatia.
- [0017 — Persistência de metadados de corrida (`run.json`)](./0017-run-metadata-persistence.md) — `RunMetadata(alpha_forge_version, timestamp_utc, command, run_id, flags: dict[str,str])` gravado em `results/validation/<run_id>/run.json` com mesmo envelope da ADR-0015; escrita **antes** do pipeline (rastro sobrevive abort); `flags` é `dict[str,str]` — Union heterogêneo rejeitado; sem host info, sem hash, sem `inspect` subcommand, sem grid, sem comparação.
- [0018 — CLI de comparação: subcomando `compare`](./0018-validation-compare-subcommand.md) — `alpha-forge compare RUN_ID_A RUN_ID_B [--skip-*]` imprime diff humano read-only de duas corridas persistidas; quatro funções puras `_diff_*` por artefato (metadata/walk_forward/monte_carlo/cost_stress) testáveis unitariamente; saída stdout com header + 4 seções `--- nome ---` + deltas `Δ=` com sinal; exit 0 sempre (mesmo divergente), 1 se run faltando/`ValidationError`, 2 se flag inválida; JSON/ranking/N≥2 runs rejeitados.
- [0019 — Spread sintético como terceiro componente de custo](./0019-synthetic-spread-cost-component.md) — `CostModel.spread_bps: float ≥ 0 default 0.0` aditivo em `total_bps` de `apply_cost` (ADR-0006 estendida); `CostPerturbation.spread_delta_bps` opcional; CLI ganha `--spread-bps` compartilhada e `--stress label:fee:slip[:spread]` (3 ou 4 partes, ≥5 rejeitado); `schema_version` inalterado (pydantic default absorve payloads antigos); monotonicidade ADR-0010 estendida ao componente spread via property-based isolado; funding e maker/taker continuam fora (ADRs futuras).
- [0020 — Agentic overlay import](./0020-agentic-overlay-import.md) — importa overlay agentic do fork (`feature/agentic-pilot-donchian`): 5 subagentes em `.claude/agents/`, 3 hooks em `.claude/hooks/` (`block_live_trading.py` PreToolUse, `session_reminder.py` SessionStart, `check_gates.py` Stop), `.claude/settings.json`, `scripts/validate_artifacts.py` e protocolo de 6 artefatos (SPEC/IMPLEMENTATION/VALIDATION/BACKTEST/AUDIT/CHECKLIST) como templates em `agentic/templates/`; pilotos vivem em `agentic/active/<slug>/` e são opt-in (`check_gates.py` e `validate_artifacts.py` retornam 0 se `agentic/active/` vazio); **não importa** `src/` do fork (nosso está à frente: ADRs 0012–0019), nem ADRs do fork (nosso histórico é fonte de verdade), nem CI (ADR-0021 futura), nem configs declarativas (ADR-0022 futura); overlay toca apenas `.claude/`, `scripts/`, `agentic/`, `decisions/` — zero mudança em `src/` (suíte inalterada: 289 passed/1 skipped).
- [0021 — CI agentic: estender `ci.yml` com gates de ADR-0020](./0021-ci-agentic-workflow.md) — adiciona 2 steps em `.github/workflows/ci.yml` após o step `pytest`: `uv run python scripts/validate_artifacts.py` (cobra artefatos de pilotos ativos via ADR-0020; opt-in, exit 0 em repo sem piloto) e gate anti-hardcode `grep -rE '\b(BTC|ETH|SOL)\b' src/` (exit 1 se match; hoje 0 matches, invariante da ADR-0009 §2-ter); **não** cria workflow separado (reuso do setup `uv sync` já existente; ~2s overhead total); **não** bumpa Python para 3.13 (projeto declara `>=3.12`; discussão separada); sem secrets, sem matrix de OS, sem cache adicional, sem `continue-on-error`; 10 alternativas explicitamente rejeitadas (workflow separado, job paralelo, grep como teste pytest, yamllint local, hook live-trading no CI, Python 3.13, cache pip/uv, badge README, pre-commit em vez de CI, matrix windows/macos).
- [0023 — Composite regime filter (AND/OR)](./0023-composite-regime-filter.md) — estende ADR-0022 com combinador `CompositeFilter(filters, mode: and|or)` cumprindo o mesmo Protocol `RegimeFilter`; canonicalização lexicográfica dos filtros internos; parser CLI `and(f1,f2)` / `or(f1,f2)` sem aninhamento (escopo inicial 1 nível); 4 property-based obrigatórios (AND-restritivo, OR-permissivo, lookahead herdado, comutatividade canônica). Primeira hipótese multi-dimensional do protocolo (direção AND volatilidade). Primeiro consumidor: piloto H.5 `donchian-20-10-btc-180d-regime-sma-and-atr`.
- [0022 — Regime filter minimal contract](./0022-regimes-minimal-filter.md) — abre `src/alpha_forge/regimes/` com interface `RegimeFilter` Protocol (`name: str`, `is_active(window) -> bool`, causal por construção igual a `Strategy.decide`) + implementação concreta `SMASlopeFilter(window, min_slope_bps)`; engine `run_backtest` ganha parâmetro opcional `regime_filter: RegimeFilter | None = None` (default preserva bit-a-bit); coerção HOLD/EXIT quando filtro retorna `False`; CLI `validate` ganha flag `--regime-filter name:k=v:k=v` opt-in; 3 property-based obrigatórios (neutrality, lookahead, monotonicity de `min_slope_bps` vs `trade_count`); `schema_version` inalterado; `compare` retrocompatível via `flags diff`. **Motivada por 4 pilotos agentic refutados** (H.1/H.2a/H.2b/H.2c) apontando regime como fator dominante; primeiro consumidor real é piloto H.3 (Donchian+SMA filter). Explicitamente **não** implementa os 8 regimes de `vision/02-scope.md`; ML classifier permanece deferred; `strategies` não depende de `regimes` (direção unidirecional preservada).
- [0028 — Aprovação de deploy: Bollinger 30/1.5 long-only + BollingerWidthFilter(250) (Série AZ)](./0028-bollinger-width-regime-deploy-approval.md) — aprova 3 pilotos (AZ.1 ETH-2024H1, AZ.2 BTC-2024H2, AZ.4 SOL-2024H2) para export em `exports/approved/bollinger_width_regime_20260418.json` e handoff ao BotBinance; AZ.3 BTC-2025H1 excluído (23 trades < 30); divergência explícita do gate #3 do AGENTS.md (baseline 0.14% < 0.20%) registrada — racional: stress `fee+10` (0.34% round-trip) passa com `ratio_min ≥ 0.95` nos 3 aprovados; Sharpe OOS anualizado 1.56/1.83/2.40, MDD OOS 1.82–3.37%, trades 30/38/69; escopo restrito às 3 janelas aprovadas, expansão requer nova ADR; primeiro handoff formal Alpha Forge → BotBinance.
- [0030 — Contrato runtime-faithful do manifest: 4 invariantes obrigatórios](./0030-manifest-runtime-faithful-contract.md) — formaliza que todo manifest `exports/approved/*.json` assume 4 invariantes de runtime: (1) entry fill `market@open[t+1]`, (2) exit fill `market@open[t+1]`, (3) sizing `fixed_notional` literal do `execution_hints.notional_per_trade_quote_ccy`, (4) `stop_loss: disabled`; v3+ deve declarar `runtime_contract: "faithful"` + `runtime_invariants: {...}` explícitos; motivada por handoff BotBinance 2026-04-18 onde 4 reinterpretações erradas (double-strip causal, limit-at-trigger com survivor bias, sizing 4.4× via 2% risk, stop loss ativo) inflaram Sharpe +6 a +8 e PnL +990% que sumiu após fixes; manifests v1/v2 ficam imutáveis com conformidade implícita; runtimes genéricos precisam de adapter dedicado por family; `faithful` é o único contrato nomeado hoje (outros contratos ex: `trailing_stop` ficam em ADR futura).
- [0031 — JSON-schema formal dos manifests (v3+)](./0031-manifest-json-schema.md) — emite `exports/approved/manifest.schema.json` (Draft 2020-12) como validator canônico dos manifests v3+; v1/v2 ficam fora do escopo (preservados imutáveis); campos obrigatórios incluem `manifest_version: "v3"`, `runtime_contract: "faithful"`, `runtime_invariants` (ADR-0030), `approved_combos` com todos OOS stats sem default; `additionalProperties: false` no topo; enforcement via `src/alpha_forge/exports/schema.py::validate_manifest` chamado no CLI de export; property-based teste `tests/exports/test_manifest_schema.py` com mutações negativas; pedido explícito do BotBinance na bridge para eliminar ambiguidade cross-agent; migração v1/v2 → v3 explicitamente fora (manifests históricos são evidência imutável).
- [0029 — Expansão de deploy: ETH 2025-H1 entra em aprovados (manifest v2)](./0029-eth-2025h1-deploy-approval.md) — Série BJ fechou matriz 3×5 (15 combos); ETH 2025-H1 passa 4 gates formais (36 trades, Sharpe 1.21, MDD 4.37%, PnL +3.71%); emite `bollinger_width_regime_20260418_v2.json` com 4 combos totais (ETH 2024H1+2025H1, BTC 2024H2, SOL 2024H2), preservando v1 imutável; adiciona `disallow_sizing_modes: [snowball, kelly_like, martingale]` após BotBinance reportar que snowball degrada ETH (+19% → +0.78% PnL); BTC 2025-H1 fica próximo (Sharpe 1.89) mas reprovado por trades=23; 11 combos excluídos formalmente; taxa de aprovação 4/15 = 27% — edge é específico de ativo × período, não universal.
